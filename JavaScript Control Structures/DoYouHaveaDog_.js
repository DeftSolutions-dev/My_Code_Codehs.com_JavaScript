/* This program should declare a boolean that describes whether or
 * not you have a dog. Then you should print out an informative
 * message to the user. */
function start(){
	var hasDog = false;
	println("Has a dog? "+hasDog);
}